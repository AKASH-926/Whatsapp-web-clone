import axios from 'axios'

export const AddUser = async (data) => {
    // try {

    //     await axios.post(url, data)

    // } catch (error) {
    //     console.log('erorr occurred during adduser', error)
    // }
}